/**
 * Provides the classes necessary to create and solve boggle games,
 * both at the command-line and through a local webserver.
 *
 * (Package descriptions like this appear in a file called
 * "package-info.java", at the root of the package.)
 */
package edu.brown.cs.student.boggle;
